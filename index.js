import { AppRegistry } from 'react-native';
import App from './App'; // Adjust the path if necessary
import { name as appName } from './app.json'; // Adjust the path if necessary

AppRegistry.registerComponent(appName, () => App);